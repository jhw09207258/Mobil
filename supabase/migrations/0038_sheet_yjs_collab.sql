-- ============================================================================
-- 시트(스프레드시트) 실시간 협업을 "전체 스냅샷 브로드캐스트"(마지막 저장이
-- 이김)에서 Yjs 기반 셀 단위 CRDT 로 전환하기 위한 저장 컬럼.
-- ----------------------------------------------------------------------------
-- documents/code_files 가 0028 에서, mind_maps 가 0035 에서 받은 yjs_state
-- 컬럼과 동일한 역할 — Y.encodeStateAsUpdate 로 인코딩한 CRDT 상태를 base64
-- 텍스트로 저장해, 다음 접속자가 이 시점부터 이어서 동기화할 수 있게 한다.
-- 기존 data(JSON 스냅샷) 컬럼은 그대로 두고 계속 함께 저장한다 — 내보내기/
-- 미리보기/검색 인덱스가 전부 data 를 직접 읽으므로 그 경로를 바꿀 필요가
-- 없다. realtime_topic_viewable/editable 은 0031 에서 이미 sheet: 접두사를
-- 처리하므로 추가 함수 변경은 필요 없다.
-- ============================================================================

alter table public.sheets add column yjs_state text;
